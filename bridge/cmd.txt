echo "marqueur-persistence-XYZ-2026" > /tmp/preuve-persistence.txt
{
  echo "===== RUN 1 : creation du marqueur ====="
  echo "hostname : $(hostname)"
  echo "uptime   : $(uptime -p) (boot: $(uptime -s))"
  echo "fichier cree :"
  ls -la /tmp/preuve-persistence.txt
  cat /tmp/preuve-persistence.txt
} > bridge/out.txt 2>&1
exit 0
